name: Tests

on:
  push:
    branches: ['**']
  workflow_dispatch:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    # Deliberately unfiltered: npm test carries the cross-platform lockstep
    # guards (tests/protocol-{android,swift}-parity.test.js,
    # tests/i18n-android-parity.test.js), which read the native sources under
    # android/ and appletv/, so a native-only push must still run it.
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'npm'
      - run: npm ci
      - run: npm test

  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    container:
      # Keep this tag in sync with @playwright/test in package.json, which is
      # pinned exactly (no caret) for that reason: a floating range can resolve
      # past the newest published image. MCR lags the npm release — check
      # https://mcr.microsoft.com/v2/playwright/tags/list for a stable
      # v<version>-noble tag before bumping the package.
      image: mcr.microsoft.com/playwright:v1.61.1-noble
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'npm'
      - run: npm ci
      - name: Run E2E tests
        run: npx playwright test --project=e2e --project=e2e-airconsole
