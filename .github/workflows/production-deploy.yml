name: Production Deploy

on:
  push:
    tags:
      # Bare semver, the repo's release tag convention (matching release.yml).
      # The old '[0-9]*' also matched things like a 2026-experiment tag.
      - '[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: read

jobs:
  # Tag pushes don't match the Tests workflow's branch filter, so without this
  # the rollout to users was the one deploy nothing gated. Runs alongside the
  # image build, so it costs no wall clock the build wasn't already spending.
  tests:
    uses: ./.github/workflows/test.yml
    permissions:
      contents: read

  build:
    uses: ./.github/workflows/docker-publish.yml
    secrets: inherit
    permissions:
      contents: read
      packages: write
      id-token: write

  deploy:
    runs-on: [self-hosted, k8s]
    needs: [tests, build]
    environment: production
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Install kubectl
        uses: azure/setup-kubectl@v5
        with:
          version: v1.30.0

      - name: Write kubeconfig
        run: |
          echo "${KUBECONFIG_B64}" | base64 -d > kubeconfig
          echo "KUBECONFIG=$PWD/kubeconfig" >> "$GITHUB_ENV"
        env:
          KUBECONFIG_B64: ${{ secrets.KUBECONFIG_B64 }}

      - name: Deploy to production
        env:
          TAG: ${{ github.ref_name }}
          GIT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          REPO=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/${REPO}:${TAG}"

          echo "Deploying ${TAG} to production"
          # Single patch = single rollout; separate `set image` + `set env`
          # calls each trigger their own pod replacement.
          kubectl patch deployment/couch-games -n default --patch "
          spec:
            template:
              spec:
                containers:
                  - name: couch-games
                    image: '${IMAGE}'
                    env:
                      - name: APP_ENV
                        value: production
                      - name: GIT_SHA
                        value: '${GIT_SHA}'
          "
          kubectl rollout status deployment/couch-games -n default --timeout=180s

          {
            echo "### Production Deploy ${TAG}"
            echo "- Image: \`${IMAGE}\`"
          } >> "$GITHUB_STEP_SUMMARY"
